@diagram demo_idle_vs_peak_scatter

chrome
  use scatter_360
end chrome

scatter
  render = "chartjs"
  x = idle_minutes as "Idle minutes"
  y = peak_concurrent_apps as "Peak apps"
  size = intensity as "Intensity"
end scatter
