@diagram demo_activity_5min_line

include presentation "../presentations/line-right-360.dashpresentation"

transform series {
  max = 6
  other = "Other"
}

diagram line {
  render = "chartjs"
  x = bucket_start_utc
  x_format = time.short
  x_step = 5m
  y = event_count
  scale_y = integer
  series = app_name
}
