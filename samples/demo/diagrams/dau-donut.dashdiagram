@diagram demo_dau_donut

chrome
  use donut_right_360
end chrome

series
  max = 8
  other = "Other"
end series

donut
  render = "chartjs"
  category = app_name as "Product"
  value = distinct_users as "Users"
  scale_value = integer
  order_by = "distinct_users DESC, app_name"
end donut
