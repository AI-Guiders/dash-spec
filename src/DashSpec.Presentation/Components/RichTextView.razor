@namespace DashSpec.Presentation.Components
@using DashSpec.Execution.Runtime

<span class="rich-text">@((MarkupString)CreoleSubset.ToHtml(Text))</span>

@code {
    [Parameter]
    public string? Text { get; set; }
}
